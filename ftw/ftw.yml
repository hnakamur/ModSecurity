---
logfile: "/var/log/modsecurity/audit/audit.log"
maxmarkerretries: 10
testoverride:
  input:
    dest_addr: crs-nginx
    port: 8080
  ignore:
    920100-4: "NGINX rejects request with 400 bad request, Coraza not reached"
    920100-8: "NGINX allows a colon in the path. Test expects status 400 or 403 (Apache behaviour)"
    920100-14: "NGINX rejects request with 400 bad request, Coraza not reached"
    920100-16: "NGINX accepts request with 200 OK"
    920181-1: "NGINX rejects a request with having Transfer-Encoding chunked and Content-Length"
    920270-4: "NGINX accepts a request with host containing %"
    920274-1: "NGINX accepts a request with host containing %"
    920290-4: "NGINX accepts a request with an empty host"
    920400-1: "We don't set MAX_FILE_SIZE; should be checked with SecRequestBodyLimit"
    920410-1: "We don't set COMBINED_FILE_SIZES; should be checked with SecRequestBodyLimit"
    920410-2: "We don't set COMBINED_FILE_SIZES; should be checked with SecRequestBodyLimit"
    920430-8: "NGINX rejqcts a HTTP/4.0 request"
    920610-2: "NGINX accepts a URI with a fragment"
    920620-1: "NGINX does not concatinate two Content-Type request headers"
    933220-2: "The header name `X_Filename` is considered invalid and is removed by nginx"
    933220-4: "The header name `X.Filename` is considered invalid and is removed by nginx"
    933110-3: "NGINX removes headers containing a underscore in the default settings"
    933110-13: "NGINX removes headers containing a underscore in the default settings"
    933110-14: "NGINX removes headers containing a underscore in the default settings"
    933110-20: "NGINX removes headers containing a dot in the default settings"
    933110-21: "NGINX removes headers containing a dot in the default settings"
    933110-22: "NGINX removes headers containing a dot in the default settings"
    933110-24: "NGINX removes headers containing a dot in the default settings"
    933110-25: "NGINX removes headers containing a dot in the default settings"
    933110-26: "NGINX removes headers containing a dot in the default settings"
    933110-27: "NGINX removes headers containing a dot in the default settings"
    942131-3: "ModSecurity v3 log bug, the first part of the chain always produces a log https://github.com/owasp-modsecurity/ModSecurity/issues/2672"
    942131-5: "ModSecurity v3 log bug, the first part of the chain always produces a log https://github.com/owasp-modsecurity/ModSecurity/issues/2672"
    942131-7: "ModSecurity v3 log bug, the first part of the chain always produces a log https://github.com/owasp-modsecurity/ModSecurity/issues/2672"
    944100-11: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944100-12: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944100-15: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944100-16: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944110-11: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944110-12: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944110-15: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944110-16: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-6: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-7: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-23: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-24: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-40: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-41: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-57: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-58: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-74: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-75: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-91: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-92: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-108: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-109: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-125: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944120-126: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944140-3: "The header name `X_Filename` is considered invalid and is removed by nginx"
    944140-8: "The header name `X_Filename` is considered invalid and is removed by nginx"
    944210-6: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944210-7: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944210-23: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944210-24: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944210-40: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    944210-41: "`REQUEST_BODY` is always present, and rule catches it in case of CT application/xml. A libmodsecurity3 plugin could solve this"
    980170-1: "libmodsecurity3 does not make any log entry about rule triggering in error.log if the rule has `noauditlog` action, like 980170"
    980170-2: "libmodsecurity3 does not make any log entry about rule triggering in error.log if the rule has `noauditlog` action, like 980170"
